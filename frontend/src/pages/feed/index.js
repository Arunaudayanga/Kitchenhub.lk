import Feed from '../../components/Feed/Feed';

const FeedPage = () => {
  // This page simply renders the Feed component within the MainLayout
  // The MainLayout will be applied via _app.js
  return <Feed />;
};

export default FeedPage;